import setuptools

setuptools.setup(
    name="bkgames",
    version="0.0.1",
    description="Bkgames finds teams to watch",
    packages=setuptools.find_packages('src'),
    package_dir={'': 'src'}
)
